import React from "react";
import ReactDOM from "react-dom/client";
import SiteShellPrototype from "./site-shell-prototype";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SiteShellPrototype />
  </React.StrictMode>
);
